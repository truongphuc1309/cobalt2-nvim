# News

This fork has been converted from TokyoNight.nvim into a single Cobalt2 theme.

- Public API: `colorscheme cobalt2`, `require("cobalt2")`
- Theme variants and generated extras were removed.
- Plugin highlight coverage from TokyoNight.nvim is retained and recolored.
